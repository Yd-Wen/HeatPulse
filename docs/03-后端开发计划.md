# HeatPulse 后端开发计划

## 阶段 1：项目初始化与基础架构

### 1.1 初始化 Express + TypeScript 后端项目
- 创建 backend 目录
- 初始化 package.json，安装依赖：
  - `npm install express@5 prisma @prisma/client ws node-cron nodemailer axios cheerio dotenv cors`
  - 开发依赖: `npm install -D typescript @types/express @types/node @types/ws @types/node-cron @types/nodemailer ts-node nodemon prisma`
- 配置 TypeScript (tsconfig.json)
- 创建 src 目录结构

### 1.2 创建数据库层 (Prisma)
- 初始化 Prisma: `npx prisma init --datasource-provider sqlite`
- 创建 Prisma Schema (prisma/schema.prisma)
- 定义数据模型: Keyword, Hotspot, Notification
- 运行迁移: `npx prisma migrate dev --name init`
- 创建 Prisma Client 单例 (src/utils/prisma.ts)

### 1.3 创建 Express 应用入口
- 配置 app.ts (中间件、路由、错误处理)
- 创建 server.ts (服务入口)

## 阶段 2：API 路由实现

### 2.1 关键词管理 API
- `GET /api/keywords` - 获取所有关键词
- `POST /api/keywords` - 创建关键词
- `PATCH /api/keywords/:id` - 更新关键词
- `DELETE /api/keywords/:id` - 删除关键词

### 2.2 热点管理 API
- `GET /api/hotspots` - 获取热点列表（支持分页、筛选）
- `GET /api/hotspots/:id` - 获取热点详情
- `POST /api/hotspots/:id/read` - 标记已读

### 2.3 扫描控制 API
- `POST /api/scan/trigger` - 手动触发扫描
- `GET /api/scan/status` - 获取扫描状态

### 2.4 统计 API
- `GET /api/stats` - 获取统计数据

## 阶段 3：核心业务逻辑

### 3.1 扫描服务 (Scanner Service)
- 创建扫描主逻辑 (services/scanner/index.ts)
- 实现数据源接口：
  - 网页搜索爬虫 (sources/web.ts)
  - Twitter API 集成 (sources/twitter.ts)
- 创建 AI 分析器 (services/scanner/analyzer.ts)

### 3.2 AI 集成 (OpenRouter + DeepSeek)
- 创建 OpenRouter 封装 (services/ai/openrouter.ts)
- 创建 Prompt 模板 (services/ai/prompts.ts)
- 实现热点分析逻辑：
  - 真实性判断
  - 重要性评分
  - 摘要生成
  - 标签提取

### 3.3 WebSocket 服务
- 创建 WebSocket 服务器 (services/websocket.ts)
- 实现广播功能：
  - 新热点推送
  - 扫描状态推送
- 心跳机制

## 阶段 4：通知与定时任务

### 4.1 邮件通知服务
- 配置 Nodemailer SMTP
- 创建邮件模板
- 实现发送逻辑

### 4.2 定时扫描任务
- 配置 node-cron 定时任务
- 实现每 30 分钟自动扫描
- 扫描结果处理流程

## 阶段 5：环境与配置

### 5.1 环境变量
- 配置 .env 文件模板
- 环境变量验证

## 关键文件路径

```
backend/
├── src/
│   ├── app.ts                    # Express 应用配置
│   ├── server.ts                  # 服务入口
│   ├── routes/
│   │   ├── keywords.ts           # 关键词路由
│   │   ├── hotspots.ts           # 热点路由
│   │   ├── scan.ts               # 扫描路由
│   │   └── stats.ts              # 统计路由
│   ├── services/
│   │   ├── ai/
│   │   │   ├── openrouter.ts     # OpenRouter 封装
│   │   │   └── prompts.ts        # Prompt 模板
│   │   ├── scanner/
│   │   │   ├── index.ts          # 扫描主逻辑
│   │   │   ├── analyzer.ts       # AI 分析器
│   │   │   └── sources/
│   │   │       ├── web.ts        # 网页爬虫
│   │   │       └── twitter.ts    # Twitter API
│   │   ├── websocket.ts          # WebSocket 服务
│   │   └── email.ts              # 邮件服务
│   ├── jobs/
│   │   └── scanCron.ts           # 定时任务
│   ├── utils/
│   │   └── prisma.ts             # Prisma Client 单例
│   └── types/
│       └── index.ts              # 类型定义
├── prisma/
│   ├── schema.prisma             # 数据库模型
│   └── migrations/                # 迁移文件
├── data/                         # SQLite 数据库文件
├── package.json
├── tsconfig.json
└── .env                          # 环境变量
```

## 验证方式

1. **数据库验证**: `npx prisma studio` 查看数据表结构正确
2. **API 验证**: 使用 curl 测试所有 API 端点
3. **前端集成验证**: 启动后端后，前端能正常请求数据
4. **WebSocket 验证**: 前端能收到实时推送
5. **扫描功能验证**: 手动触发扫描，检查热点是否生成

## 注意事项

- 使用 Prisma ORM 替代 better-sqlite3 原生查询
- 爬虫实现需要注意频率控制，避免被封
- AI 调用需要处理错误和重试逻辑
- WebSocket 需要处理连接断开和重连
- Prisma 需要运行 migrate 后才能使用